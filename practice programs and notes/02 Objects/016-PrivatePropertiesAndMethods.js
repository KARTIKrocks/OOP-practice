function Circle(radius) {
  
  this.radius = radius;
  let defaultLocation = { x: 0, y: 0 };
  let computerOptimumLocation = function (factor) {

  }
  this.draw = function () {
    let x,y;
    computerOptimumLocation(0.1);
    //defaultLocation
    //this.radius 
    console.log('draw');
  };
}
const circle = new Circle(10);
circle.                               // CTRL+space and check there are onlyu two methods!!
circle.draw();