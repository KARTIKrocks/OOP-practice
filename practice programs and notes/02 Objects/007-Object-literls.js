const circle = {
  radius: 1,
  location: {
    x: 1,
    y: 1
  },
  draw: function () {
    console.log('draw');
  }
};    // this circle object has three menbers -radius, location, draw . and we will call radius and location 'property' and draw 'method' .

circle.draw();